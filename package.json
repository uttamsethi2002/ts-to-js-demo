
{
  "name": "lambda-ts",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "clean": "rm -rf dist",
    "build": "esbuild src/lambda_function.ts --bundle --platform=node --target=node20 --outfile=dist/lambda_function.js"
  },

  "devDependencies": {
    "typescript": "^5.9.3",
    "esbuild": "^0.20.2",
    "@types/aws-lambda": "^8.10.160"
  }
}
